


Type t1 = DateTime.Now.GetType(); // Get type in RunTime

Type t2 = typeof(DateTime);// Get type in CompileTime


//Activator.CreateInstance(typeof());   return Object Handler
// to change it to object we use .Unwrap() 

